<%= form_for @project do |f| %>
<%= f.label :title, "Project Title" %>
<%= f.text_field :title %>
<%= f.label :goal, "Project Goal" %>
<%= f.number_field :goal %>
<%= f.collection_select :creator_id, User.all, :id, :name %>
<%= f.submit "Make this Project" %>
<%end%>
